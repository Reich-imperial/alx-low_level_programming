#include <stdio.h>

/**
 * main - Prints the alphabets in lowercase, and then in uppercase
 *
 * Return: 0
 */

int main(void)
{
	char letter;

	for (letter = "a"; letter <= "z"; ch++)
	{
		putchar(letter);
	}
	for (letter = "A"; letter <= "Z"; ch++)
	{
		putchar(letter);
	}
	putchar("\n");
	return (0);
}
